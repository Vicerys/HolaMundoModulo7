<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="Alertas01.MainPage">

    <ScrollView>
        <VerticalStackLayout
            Padding="30,0"
            Spacing="25">
            <Image
                Source="dotnet_bot.png"
                HeightRequest="185"
                Aspect="AspectFit"
                SemanticProperties.Description="dot net bot in a submarine number ten" />

            <Label
                Text="Hello, World!"
                Style="{StaticResource Headline}"
                SemanticProperties.HeadingLevel="Level1" />

            <Label
                Text="Welcome to &#10;.NET Multi-platform App UI"
                Style="{StaticResource SubHeadline}"
                SemanticProperties.HeadingLevel="Level2"
                SemanticProperties.Description="Welcome to dot net Multi platform App U I" />

            <Button
                x:Name="AlertBtn"
                Text="Button alert" 
                Clicked="ButtonAlert_Clicked" />
            <Button
                x:Name="DecisionBtn"
                Text="Button decisiones" 
                Clicked="ButtonAlert2_Clicked" />
            <Button
                x:Name="NameBtn"
                Text="Button Nombre desde toast" 
                Clicked="ButtonAlert3_Clicked" />
            <Button
                x:Name="DisplayBtn"
                Text="Button Display" 
                Clicked="ButtonDisplayActionSheet_Clicked" />
            <Button
                x:Name="ToastBtn"
                Text="Button Toast" 
                Clicked="ButtonToast_Clicked" />
        </VerticalStackLayout>
    </ScrollView>

</ContentPage>
